---
all:
  children:
    host:
      hosts: localhost
      vars:
        ansible_connection: local
        ansible_python_interpreter: "{{ ansible_playbook_python }}"
        ansible_user: "{{ secret_username }}"
        ansible_become_password: "{{ secret_password }}"
